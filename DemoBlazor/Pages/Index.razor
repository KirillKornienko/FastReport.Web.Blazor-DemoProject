@page "/"
@page "/{ReportName}"

<WebReportContainer WebReport="@MyWebReport" />

@code {
    [Parameter]
    public string ReportName { get; set; } = string.Empty;

    public WebReport? MyWebReport { get; set; } = default(WebReport);
}